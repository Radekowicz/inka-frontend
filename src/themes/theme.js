import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      // main: '#5856D6',
      main: '#19afb0',
      contrastText: '#fff',
    },
    secondary: {
      main: '#5856D6',
    },
  },
});

export default theme;
